// Copia este archivo a config.js y completa los valores reales antes de desplegar.
// Para pruebas locales con el proxy, usa la propiedad `secureEndpoint`.
// No metas la `SERVICE_ROLE_KEY` en este archivo: solo en el proxy (.env).
window.SUPABASE_CONFIG = {
  // Supabase (opcional si usas proxy local)
  url: 'https://wfiyucykjoohdiazlqbz.supabase.co',
  anonKey: 'sb_publishable_wlIN6gMmG_pVr-h-MAaLOw_jUyW4pmB',

  // Ejemplo de endpoint local que usa el proxy `local_server`
  // secureEndpoint: 'http://localhost:9000/submit'
};
